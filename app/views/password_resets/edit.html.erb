<%= form_with model: @user, url: password_reset_path, local: true, method: :patch do |form| %>

  <%= form.hidden_field :reset_token %>

  <%= form.label :password %>
  <%= form.password_field :password, id: :password %>

  <%= form.submit "Update Password", id: :update_password_submit %>
<% end %>

